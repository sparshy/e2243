%% part 1 checking if a vector x in R^m is a linear combination of x_1, x_2 ... x_n
% if m = n we can try solving Ax = b, if A is invertible we can yes 
% if m > n we can not  
% if m < n and A has full row rank then yes